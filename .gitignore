# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
build/
*.ipa

# vcpkg — only the pre-built arm64-ios libs are committed
vcpkg/buildtrees/
vcpkg/downloads/
vcpkg/packages/
vcpkg/toolsrc/
vcpkg/docs/
vcpkg/ports/
vcpkg/scripts/
vcpkg/versions/
vcpkg/vcpkg
vcpkg/*.log

# macOS
.DS_Store
*.swp
new-sounds/
ios-build/
.build/
vcpkg/

# Secrets
.env
.env.*
!.env.example

# Claude Code per-developer state. `settings.local.json` follows the
# `.local` convention — per-machine tool allowlists, never team-shared.
# A team-wide `settings.json` would be tracked; this repo doesn't use one.
.claude/settings.local.json

# Python build artifacts
__pycache__/
*.pyc
tmp_overlays/

# session-handoff skill scratch — within-seat clear/resume only, never landed
HANDOFF.md
# Held DECISIONS.md draft entries, pending David's sign-off — scratch,
# never landed to docs/DECISIONS.md without explicit sign-off per entry.
DECISIONS_DRAFT.md
